// Legacy university page — replaced by the UniversityDrawer slide-in.
// Kept as a stub to avoid orphan-file type errors; routing no longer mounts it.
export default function UniversityStub() {
  return null
}